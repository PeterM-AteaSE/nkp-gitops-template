---
# Source: external-dns/crds/dnsendpoints.externaldns.k8s.io.yaml
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    api-approved.kubernetes.io: https://github.com/kubernetes-sigs/external-dns/pull/2007
  name: dnsendpoints.externaldns.k8s.io
spec:
  group: externaldns.k8s.io
  names:
    kind: DNSEndpoint
    listKind: DNSEndpointList
    plural: dnsendpoints
    singular: dnsendpoint
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: |-
            DNSEndpoint is a contract that a user-specified CRD must implement to be used as a source for external-dns.
            The user-specified CRD should also have the status sub-resource.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: DNSEndpointSpec defines the desired state of DNSEndpoint
              properties:
                endpoints:
                  items:
                    description: Endpoint is a high-level way of a connection between a service and an IP
                    properties:
                      dnsName:
                        description: The hostname of the DNS record
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels stores labels defined for the Endpoint
                        type: object
                      providerSpecific:
                        description: ProviderSpecific stores provider specific config
                        items:
                          description: ProviderSpecificProperty holds the name and value of a configuration which is specific to individual DNS providers
                          properties:
                            name:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      recordTTL:
                        description: TTL for the record
                        format: int64
                        type: integer
                      recordType:
                        description: RecordType type of record, e.g. CNAME, A, AAAA, SRV, TXT etc
                        type: string
                      setIdentifier:
                        description: Identifier to distinguish multiple records with the same name and type (e.g. Route53 records with routing policies other than 'simple')
                        type: string
                      targets:
                        description: The targets the DNS record points to
                        items:
                          type: string
                        type: array
                    type: object
                  type: array
              type: object
            status:
              description: DNSEndpointStatus defines the observed state of DNSEndpoint
              properties:
                observedGeneration:
                  description: The generation observed by the external-dns controller.
                  format: int64
                  type: integer
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}

---
# Source: external-dns/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: externaldns-internal-external-dns
  namespace: platform-externaldns
  labels:
    helm.sh/chart: external-dns-1.20.0
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: externaldns-internal
    app.kubernetes.io/version: "0.20.0"
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: true
---
# Source: external-dns/templates/clusterrole.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: externaldns-internal-external-dns
  labels:
    helm.sh/chart: external-dns-1.20.0
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: externaldns-internal
    app.kubernetes.io/version: "0.20.0"
    app.kubernetes.io/managed-by: Helm
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["list","watch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get","watch","list"]
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["get","watch","list"]
  - apiGroups: ["discovery.k8s.io"]
    resources: ["endpointslices"]
    verbs: ["get","watch","list"]
  - apiGroups: ["extensions","networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get","watch","list"]
---
# Source: external-dns/templates/clusterrolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: externaldns-internal-external-dns-viewer
  labels:
    helm.sh/chart: external-dns-1.20.0
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: externaldns-internal
    app.kubernetes.io/version: "0.20.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: externaldns-internal-external-dns
subjects:
  - kind: ServiceAccount
    name: externaldns-internal-external-dns
    namespace: platform-externaldns
---
# Source: external-dns/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: externaldns-internal-external-dns
  namespace: platform-externaldns
  labels:
    helm.sh/chart: external-dns-1.20.0
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: externaldns-internal
    app.kubernetes.io/version: "0.20.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: externaldns-internal
  ports:
    - name: http
      port: 7979
      targetPort: http
      protocol: TCP
---
# Source: external-dns/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: externaldns-internal-external-dns
  namespace: platform-externaldns
  labels:
    helm.sh/chart: external-dns-1.20.0
    app.kubernetes.io/name: external-dns
    app.kubernetes.io/instance: externaldns-internal
    app.kubernetes.io/version: "0.20.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: external-dns
      app.kubernetes.io/instance: externaldns-internal
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: external-dns
        app.kubernetes.io/instance: externaldns-internal
    spec:
      automountServiceAccountToken: true
      serviceAccountName: externaldns-internal-external-dns
      securityContext:
        fsGroup: 65534
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: external-dns
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
          image: registry.k8s.io/external-dns/external-dns:v0.20.0
          imagePullPolicy: IfNotPresent
          env:
            - name: RFC2136_KERBEROS_USERNAME
              valueFrom:
                secretKeyRef:
                  key: username
                  name: externaldns-kerberos
            - name: RFC2136_KERBEROS_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: externaldns-kerberos
          args:
            - --log-level=info
            - --log-format=text
            - --interval=1m
            - --source=service
            - --source=ingress
            - --policy=sync
            - --registry=txt
            - --txt-owner-id=platform-prod01
            - --domain-filter=k8s.<CUSTOMER_DOMAIN>
            - --label-filter=<CUSTOMER_DOMAIN>/internaldns in (true)
            - --provider=rfc2136
            - --rfc2136-gss-tsig
            - --rfc2136-host=<CUSTOMER_AD_KDC>
            - --rfc2136-port=53
            - --rfc2136-zone=k8s.<CUSTOMER_DOMAIN>
            - --rfc2136-kerberos-realm=<CUSTOMER_KERBEROS_REALM>
            - --rfc2136-kerberos-username=$(RFC2136_KERBEROS_USERNAME)
            - --rfc2136-kerberos-password=$(RFC2136_KERBEROS_PASSWORD)
            - --rfc2136-tsig-axfr
          ports:
            - name: http
              protocol: TCP
              containerPort: 7979
          livenessProbe:
            failureThreshold: 2
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
          readinessProbe:
            failureThreshold: 6
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
          volumeMounts:
            - mountPath: /etc/krb5.conf
              name: kerberos-config-volume
              readOnly: true
              subPath: krb5.conf
      volumes:
        - configMap:
            defaultMode: 420
            name: krb5-conf
          name: kerberos-config-volume
